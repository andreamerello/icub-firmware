

# for ST-Link (all vers)
#source [find interface/stlink.cfg]

# for the regular square grey olimex with proper SWD adapter (not the H version)
# source [find interface/ftdi/olimex-arm-usb-ocd.cfg]


# for the yellow (and black?) Jlink
# source [find interface/jlink.cfg]

# the small triangular grey olimex with my hackish cable
source [find interface/ftdi/olimex-jtag-tiny.cfg]
# source [find interface/ftdi/swd-resistor-hack.cfg]
source [find interface/ftdi/olimex-arm-jtag-swd.cfg]
#source [find target/swj-dp.tcl]
adapter_khz 1000

source [find target/stm32g4x.cfg]
#reset_config srst_only

#$_TARGETNAME configure -event gdb-attach {
#   halt
#}


#reset_config trst_and_srst trst_push_pull
#$_TARGETNAME configure -rtos auto